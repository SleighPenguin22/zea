#![no_main]

use arbitrary::{Arbitrary, Unstructured};
use libfuzzer_sys::fuzz_target;
use zea_ipr::{visualisation::IndentPrint, zea::ipr::*};
fuzz_target!(|data: &[u8]| {
    if let Ok(mut m) = IPRModule::arbitrary(&mut Unstructured::new(data)) {
        m.label_nodes();
    }
});
