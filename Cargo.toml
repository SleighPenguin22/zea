[workspace]
resolver = "3"

members = ["zea-ast", "zea-driver", "zea-parser", "zea-codegen", "zea-internal-macros", "zea-config"]

[workspace.lints.rust]
dead_code = "allow"
unsafe_code = "forbid"
unused_imports = "allow"

[profile.release]
debug = true
