[workspace]
resolver = "3"

members = ["zea-ipr", "zea-driver", "zea-parser", "zea-codegen", "zea-internal-macros", "zea-common"]

[workspace.lints.rust]
dead_code = "allow"
unsafe_code = "forbid"
unused_imports = "allow"

[profile.release]
debug = true
