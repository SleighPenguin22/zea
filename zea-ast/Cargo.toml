[package]
name = "zea-ast"
version = "0.1.0"
edition = "2024"

[dependencies]
vizoxide = { path = "../vizoxide", optional = true }
thiserror = "2.0.18"
zea-macros = { path = "../zea-macros" }

[features]
visualisation = ["dep:vizoxide"]