module main

fn lnint(x: Int) -> Int {
    if x < 0 {
        panic(aaaaaaaaa);
    } else {
        lnint-unsafe(x)
    }
}

fn lnint(x: Int) -> Int {
    if x < 0 {
        panic(aaaaaaaaa);
    } else {
        lnint-unsafe(x)
    }
}

fn lnint(x: Int) -> Int {
    if x < 0 {
        panic(aaaaaaaaa);
    } else {
        lnint-unsafe(x)
    }
}
